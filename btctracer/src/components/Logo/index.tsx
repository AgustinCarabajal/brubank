/**
 * The `Logo` component renders a logo for a Bitcoin Tracer application.
 */
export const Logo = () => (
  <div className="logo" data-testid="logo-container">
    Bitcoin<span className="logo-color">Tracer</span>
  </div>
);
